<%= provide(:title, 'Apuestas de usuario') %>
<%= provide(:header, 'Apuestas de usuario') %>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Apostador</th>
      <th>Descripción</th>
      <th>Monto apostador</th>
      <th>Monto desafiante</th>
      <th>Start date</th>
      <th>End date</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_bets.each do |user_bet| %>
      <tr>
        <td><%= link_to user_bet.name, user_user_bet_path(@user, user_bet) %></td>
        <td><%= user_bet.user_id %></td>
        <td><%= user_bet.description %></td>
        <td><%= user_bet.challenger_amount %></td>
        <td><%= user_bet.gambler_amount %></td>
        <td><%= user_bet.start_date %></td>
        <td><%= user_bet.end_date %></td>
        <% if current_user == @user %>
          <td><%= link_to 'Eliminar', user_user_bet_path(current_user, user_bet), method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<% if current_user == @user %>
  <%= link_to 'Nueva MeesBet', new_user_user_bet_path(current_user) %>
<% end %>
